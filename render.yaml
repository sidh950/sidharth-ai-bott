services:
  - type: web
    name: sidharth-content-bot
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: BOT_TOKEN
        value: YOUR_ACTUAL_BOT_TOKEN
